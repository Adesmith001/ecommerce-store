import { PlaceholderSection } from "@/components/shared/placeholder-section";

export default function ProductsPage() {
  return (
    <div className="app-shell">
      <PlaceholderSection
        eyebrow="Storefront"
        title="Product listing placeholder"
        description="Use this route later for product grids, filters, sorting, and search."
      />
    </div>
  );
}
